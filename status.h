#ifndef STATUS_H
#define STATUS_H

enum status { FAILURE, SUCCESS }; //failure is 0, success is 1
typedef enum status Status;

enum boolean { FALSE, TRUE };
typedef enum boolean Boolean;

enum bank { LEFT, RIGHT };
typedef enum bank Bank;

#endif
